export const metadata = {
    "image": "/img/fouc-timetrial.png",
}

# v2.00

- Added support for opponent ghosts, putting a replay into `Ghosts/Opponents` will make it show up as a second ghost independent of your PB
- Changed the replay names to be shorter and to use car & track names instead of IDs