export const metadata = {
	"image": "/img/fouc-hlmov.png"
}

# v1.50

- Added options to display your velocity on screen
- Fixed a memory leak when playing sounds
- Fixed the view angle being offset when restarting a race
- The car's original velocity is now taken into account when enabling the mod
- The mod now uses [FreemanAPI](https://gaycoderprincess.github.io/project/freeman-api) as a shared library for the physics