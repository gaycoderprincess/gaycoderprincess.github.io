export const metadata = {
	"image": "/img/fouc-hlmov.png"
}

# v2.10

- Added a cvar for jump height in HL2 mode
- Added more surface types with unique footstep sounds
- Attempted to improve collision detection
- Fixed being able to unduck into ceilings in HL2 mode
- Fixed ducking being way too slow in HL1 mode
- Fixed mouse movements building up in the menus and affecting the initial camera angle
- Fixed stuttery movement when near other cars
- Fixed the camera clipping into walls
- Fixed the player hull not changing when ducked in HL2 mode
- Fixed various issues when toggling between HL1 and HL2 mode
- Ragdolling and resetting are now disabled when the mod is active