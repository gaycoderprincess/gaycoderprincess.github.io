# [{props.metadata.name}](/project/assetto-mwphysics)

A mod that replaces the entire game's handling model with the Most Wanted one, including all of its quirks and tiny details  
Note that the mod only replaces the handling model, meaning the collision physics have been left intact.

_Last updated: {props.metadata.lastUpdated}_