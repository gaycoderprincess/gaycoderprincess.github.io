export const metadata = {
    "image": "/img/fo2-timetrial.png",
}

# v3.30

- Added a toggle to load mismatched replays as long as they're named correctly (e.g. created with different nitro, upgrades, UC replays, etc.)
- Fixed the time trial UI appearing for a second during the loading screen