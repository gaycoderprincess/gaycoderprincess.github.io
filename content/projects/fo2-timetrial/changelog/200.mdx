export const metadata = {
    "image": "/img/fo2-timetrial.png",
}

# v2.00

- Added a way to remove props from the map
- Added a nitro multiplier into the config file
- The first lap is now saved separately from running laps