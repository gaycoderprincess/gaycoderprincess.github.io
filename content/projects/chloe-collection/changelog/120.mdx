export const metadata = {
    "image": "/img/chloe-collection-upd-120.png"
}

# v1.20

- Added pride flags as a replacement for player country flags :3
- Added a unique message if the host is changing track
- Attempted to fix sync issues when ramming players from the rear
- Fixed ping being calculated wrong with more than 1 other player in a lobby
- Fixed issues if an event track was selected right after a time trial
- Multiplayer mode now verifies gameplay-modifying plugins e.g. NoWallriding, FO2Handling
- Removed the Lag Compensation option from the Online Options menu, it is now always enabled
- Removed the copyright screen from game startup, making it load way faster